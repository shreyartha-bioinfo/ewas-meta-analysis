# Data

Place your per-cohort EWAS result files here (one CSV/TSV per cohort).
Expected columns: CpG, beta, se, p (optional).

The tutorial auto-generates simulated data and downloads the GEMAS EWAS
from Zenodo at runtime — no manual data needed to run the tutorial.
